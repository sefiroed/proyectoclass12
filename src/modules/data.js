/**
 * DATOS A MANIPULAR
 */
const productos = []; //Array de productos
const dbIDs = []; //Array de los IDs de los productos
const lastID = { lastID: 0 }; //Ultimo ID de producto utilizado

export { productos, dbIDs, lastID };
